{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Build ARBBPI_XSPEC_RUNNER",
      "type": "shell",
      "command": "mvn clean install",
      "options": {
        "cwd": "${workspaceFolder}"
      }
    }
  ]
}